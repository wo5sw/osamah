{
  "name": "queue-system-android",
  "version": "1.0.0",
  "private": true,
  "description": "Native Android wrapper for the Queue System web app — adds background location so a rep auto-joins the queue even when the app is closed/backgrounded.",
  "scripts": {
    "sync": "cap sync android",
    "open": "cap open android",
    "build:apk": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/core": "^6.1.2",
    "@capacitor/local-notifications": "^6.1.0",
    "@capacitor-community/background-geolocation": "^1.2.23"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
